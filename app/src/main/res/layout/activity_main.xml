<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.hourglass.lingaraj.databinding_included_layout.view.MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/button_click"
        android:text="Click to show data"
        android:onClick="handleButtonClick"
        />

    <TextView
       style="@style/textview_address_style"
        android:id="@+id/name"
        />

    <include
        android:id="@+id/address_container"
        layout="@layout/layout_address"/>

    <include
        android:id="@+id/other_container"
        layout="@layout/layout_hobby"
        />

</LinearLayout>
</layout>
